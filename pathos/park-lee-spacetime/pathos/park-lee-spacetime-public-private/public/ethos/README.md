# Domain: Ethos (Public - Nested Pathos)

> **Status**: Moral and Foundational Instantiation  
> **Origin**: The object instantiation of `creos`, by the `Human()`.  
> **Rule**: Authored and maintained by `Human()`. Read-only to AI as ethical grounding.

## Definition
`ethos` is the realization of `creos` into living principles, values, boundaries, and responsibilities. Where `creos` declares pure intent, `ethos` shapes how that intent manifests into ethical and systemic character.

```python
# Instantiation
class Ethos(Creos):
    instantiated_by = Human()
    role = "Ethical Foundation"
```
