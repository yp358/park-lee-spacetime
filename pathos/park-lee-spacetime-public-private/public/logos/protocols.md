# Operational Protocols (Public)

1. **Protocol of Inheritance**: Any computational construct executing under `pathos` must implement interfaces adhering strictly to schemas defined in `logos/schema/`.
2. **Protocol of Verification**: Logic within `logos` must be deterministic, transparent, and verifiable against `ethos`.
3. **Protocol of Guidance**: `logos` provides the scaffolding within which AI direction and artistic generation operate.
